# Used only with --sync-supabase on scrape_plm_part_attributes.py
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
# Service role (bypasses RLS) — keep secret; do not commit.
SUPABASE_SERVICE_ROLE_KEY=

# Organization that owns components / suppliers (UUID from profiles / organizations table)
COMPLIANCEHUB_ORG_ID=
